export const calculateLegalMoves = (chips, dice, turn)=>{

  if( !dice.length ) return [];

  //// if we're in Jail
  ////// calculate spaces in 0-5 or 23-18

  if( (turn === 'white') && () )
  

  //// else
  ////// if all dice we have, for all the chips we have, check if it's open

  
};
